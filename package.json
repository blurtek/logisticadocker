{
  "name": "muebleswow-plesk",
  "version": "1.0.0",
  "description": "MueblesWow - Panel de Logística para Plesk",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "build": "npm run build:frontend && npm run build:public && npm run build:backend",
    "build:frontend": "cd frontend && npm install && npm run build",
    "build:public": "cd public && npm install && npm run build", 
    "build:backend": "cd backend && npm install && npm run build",
    "install:all": "npm install && cd backend && npm install && cd ../frontend && npm install && cd ../public && npm install",
    "db:migrate": "cd backend && npx prisma migrate deploy",
    "db:seed": "cd backend && npx prisma db seed",
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\" \"npm run dev:public\"",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:public": "cd public && npm run dev"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "helmet": "^7.1.0",
    "bcryptjs": "^2.4.3",
    "jsonwebtoken": "^9.0.2",
    "zod": "^3.22.4",
    "@prisma/client": "^5.7.1",
    "prisma": "^5.7.1",
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  },
  "keywords": [
    "muebles",
    "logistica",
    "entregas",
    "plesk",
    "nodejs"
  ],
  "author": "MueblesWow",
  "license": "MIT"
}
